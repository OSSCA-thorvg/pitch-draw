# 계획 — 음정으로 그리는 그림 (가칭)

> SVG를 넣으면 그게 악보가 된다. 그 음을 따라 소리를 내면 그림이 그려지고, 음이 틀리면 선이 삐뚤어진다.

---

## 1. 핵심 메커니즘

여기가 이 프로젝트의 전부다. 나머지는 부속이다.

**펜은 목표 경로를 따라 저절로 진행한다. 사용자는 세로 위치(음정)만 결정한다.**

- SVG 경로를 처음부터 끝까지 한 붓 그리기 순서로 따라가며 일정 간격으로 점을 뽑는다
- 펜의 **가로 위치는 그 경로가 정한 대로** 자동으로 흐른다
- 펜의 **세로 위치는 사용자가 내는 음의 높이**가 결정한다
- 정확히 부르면 목표 경로와 겹쳐서 원래 그림이 나오고, 틀리면 그 지점에서 위아래로 벗어난다

이 방식이 중요한 이유: 시간축을 가로축이 아니라 **경로를 따라간 거리**로 잡았기 때문에, 원이든 되돌아오는 곡선이든 겹치는 선이든 전부 처리된다. 가로축을 시간으로 잡으면 한 x에 y가 여러 개인 그림을 아예 다룰 수 없다.

원을 예로 들면 — 시계방향으로 돌면서 목표 음이 올라갔다 내려온다. 사용자는 그에 맞춰 음을 올렸다 내리고, 틀리면 원이 찌그러진다.

## 2. 무엇으로 인상을 만들 것인가

기능 목록이 아니라 **심사위원이 무엇을 느끼는가**에서 역산한다. 우선순위 순.

### 가. 예측 불가능한 입력 — "아무 SVG나 던져보세요"

준비된 데모는 의심받는다. 심사위원이 자기 회사 로고를 던졌는데 그게 즉시 악보가 되면 그 순간 신뢰가 생긴다. **이 프로젝트에서 가장 값싸고 가장 강한 장치다** — SVG 파싱은 ThorVG가 해주고, 우리는 경로에서 점만 뽑는다.

### 나. 즉시성

소리를 내고 0.3초 뒤에 선이 움직이면 아마추어로 보인다. 즉각 붙으면 잘 만든 것으로 보인다. 기능 하나 더 넣는 것보다 여기 시간을 쓴다.

### 다. 마감

여백, 폰트, 전환, 로딩 상태. 사람들은 마감으로 실력을 판단한다. 기능 개수보다 압도적으로 크다.

### 라. 웃음

못하면 웃긴 게 이 아이디어의 최대 강점이다. 심사위원이 웃으면서 계속 만지작거리면 그게 최고 점수다. **실패한 결과가 지저분해 보이지 않고 재밌어 보이도록** 만드는 데 공을 들인다.

### 마. 깊이의 암시

음정 신뢰도, 목표와의 오차, 경로 점 개수 같은 내부 숫자를 조금 드러낸다. 실제 난이도와 무관하게 "안을 아는 사람"으로 보인다. 비용 대비 효과가 크다.

## 3. 기능 범위

### P0 — 이게 없으면 성립하지 않음

- 입력 장치 선택 + 음 높이 추출 (오디오 인터페이스 대응)
- SVG 파일 드롭 → 경로 추출 → 목표선으로 표시
- 펜이 경로를 따라 진행, 세로 위치는 음정
- 그려진 선이 목표선에서 벗어나는 것이 눈에 보임
- **가이드 음 재생** — 목표 멜로디를 소리로 들려준다
- 완주 후 결과 화면

> 가이드 음이 P0인 이유: 목표선을 보여주기로 했으니 "어디로 가야 하는지"는 눈에 보이지만,
> "어떤 음을 내야 그리로 가는지"는 여전히 알 수 없다. 화면 높이와 음정의 대응은 몸으로 익혀야
> 하는 감각이고, 심사장에서 30초 안에 익힐 사람은 없다. 따라 부를 소리가 없으면
> "심사위원이 직접 해본다"는 시나리오 자체가 성립하지 않는다.

### P1 — 인상을 키우는 것

- 정확도에 따른 선의 변화 (색·굵기·번짐)
- 내장 예제 그림 몇 개 (드롭할 파일이 없는 상황 대비)
- 결과 화면에서 목표 vs 내가 그린 것 겹쳐 보기
- 속도 조절 (느리게 하면 쉬워짐)

### P2 — 여유 있으면

- 정확도 점수
- 여러 서브패스 처리 (펜 들기 구간)
- 결과 저장
- 음역 조절 (남성/여성/악기)

### 하지 않을 것

서버, 계정, 랭킹, 곡 라이브러리, 모바일 대응, 다국어

## 4. 기술 리스크

| 리스크 | 왜 위험한가 | 확인 방법 |
|---|---|---|
| **SVG에서 경로 점을 어떻게 뽑나** | 이게 안 되면 "아무 SVG나"가 무너진다 = 가장 강한 인상 장치가 사라짐 | ThorVG로 파싱해 도형을 순회하는 방법과, 브라우저 SVG API로 경로 길이를 따라 점을 뽑는 방법 두 가지가 있다. 후자가 확실하고 쉬우며, 그려주는 건 여전히 ThorVG다 |
| 음 높이 추출의 지연 | 즉시성이 인상의 핵심 | 이미 확인함 — 동작한다. 남은 건 스무딩 세기 조절 |
| 임의 SVG의 음역이 제멋대로 | 경로가 세로로 너무 크거나 급격하면 사람이 못 따라 부름 | 경로의 세로 범위를 부를 수 있는 음역으로 정규화하고, 급격한 도약은 완만하게 |
| 경로가 너무 길거나 짧음 | 30초짜리 곡이 되면 지루함 | 진행 속도를 경로 길이에 맞춰 조절 |
| 서브패스가 여러 개 | 얼굴 + 눈 두 개 같은 그림 | P0에서는 가장 긴 경로 하나만 쓰고, 여러 개는 P2 |

## 5. 진행 순서

각 단계는 **눈으로 확인 가능한 상태**로 끝난다.

1. **음정 → 펜** — 소리를 내면 점이 위아래로 움직이고 지나간 자리에 선이 남는다 *(완료)*
2. **SVG → 목표선** — 파일을 떨어뜨리면 화면에 옅은 목표선이 깔린다 *(완료)*
3. **둘을 붙이기** — 펜이 목표선을 따라 진행하고, 세로만 음정이 결정한다. **여기서 게임이 성립한다** *(완료)*
4. **가이드 음** — 목표 멜로디를 들려준다. 이게 있어야 실제로 플레이가 가능해진다 *(완료)*
5. **연출과 마감** — 선의 질감, 색 변화, 결과 화면, 전환
6. **다듬기** — 스무딩 세기, 속도, 음역 정규화. 실제로 해보면서 조절

3번까지가 프로젝트의 뼈대다. 4번이 "할 수 있는 게임"과 "안 되는 게임"을 가른다. 5번이 점수를 만든다.

## 6. 발표 시나리오 (초안)

1. 내장 그림으로 한 번 시연 — 규칙이 설명 없이 이해된다
2. 일부러 틀리게 불러서 망가뜨린다 — 여기서 웃음
3. **심사위원에게 아무 SVG나 달라고 한다** — 그 자리에서 악보가 된다
4. 심사위원이 직접 해본다
5. (여력 되면) 오디오 인터페이스로 기타 연주해서 그린다

## 7. 확정된 설계 결정

### 목표선을 미리 보여준다

옅은 선으로 목표 경로를 깔아둔다. 따라 그리기 게임이 된다. 결과가 반전이 되는 재미는 포기하지만, **심사위원이 직접 만져서 완성할 수 있는 것**이 더 중요하다.

### 소리를 안 내면 펜이 멈춘다

음정이 안 잡히는 동안 펜은 그 자리에 선다. 결과적으로 **숨이 곧 시간**이 된다 — 길게 뻗은 구간은 긴 호흡이 필요하고, 숨을 고르는 동안 그림이 망가지지 않는다.

이 선택은 게임을 상당히 관대하게 만든다. 처음 잡는 사람도 완주할 수 있고, 그래서 심사위원이 끝까지 해본다. 대신 긴장감은 줄어든다 — 어려운 난이도가 필요하면 나중에 "멈추지 않는 모드"를 옵션으로 얹는다.

### 손그림·잉크 느낌으로 그린다

깔끔한 선이 아니라 붓이나 펜으로 그은 듯한 질감. 이유는 미감이 아니라 **실패가 지저분해 보이지 않게** 하기 위해서다. 같은 삐뚤빼뚤이라도 매끈한 선으로 그리면 "잘못 그린 그래프"로 보이고, 잉크 느낌이면 "웃긴 낙서"로 보인다. 4번 인상 장치(웃음)가 여기 달려 있다.

구현 방향 — 연출·마감 단계에서 확인된 것을 반영해 고쳐 적는다. 원래 적어둔 세 가지 중
둘은 ThorVG 로 그대로 되지 않는다.

- **종이와 잉크**: 스테이지를 종이색으로 뒤집고 궤적을 반투명 잉크로 긋는다. 밝은 종이 위
  반투명 잉크는 두 번 지난 자리가 저절로 진해져서, 블렌드 모드 없이도 겹침이 정보가 된다.
- **목표선은 연필 점선**: 실선이면 궤적의 "얇은 형제"로 보여서 기대값과 실측값을 그린
  그래프가 된다. 점선 위에 실선이 덮여야 "겹쳤다"가 눈에 보이는 사건이 된다.
- **음역 밖으로 삐져나감**: 클램프만 하면 아주 낮게 부른 구간이 밴드 끝을 따라 기어가는
  깔끔한 직선이 된다. 가장 웃겨야 할 순간이 가장 밋밋해진다.
- ~~진행 속도에 따른 선 굵기 변화~~ → **보류.** 재료가 틀렸다. 이 게임은 가로가 등속이라
  진행 속도가 거의 상수다. 다시 볼 때는 **세로 속도**(음정이 얼마나 빨리 움직이는가)로
  잡는다 — 옥타브 도약이 채찍 자국처럼 얇아지는 것이 가장 웃길 수 있는 물리다.
  그리고 ThorVG 의 stroke 굵기는 Shape 단위 스칼라라, 가변폭은 리본 폴리곤을 직접
  계산해야 한다. "궤적을 도형 하나로 유지하면 나중에 stroke 속성만 바꿔 얹을 수 있다"는
  전제는 **틀렸다.**
- ~~번짐 효과~~ → **제외.** 진짜 가장자리 흐림은 `Scene.gaussianBlur` 가 필요한데 이 앱의
  타입 선언에 없고, 소프트웨어 렌더러로 내려가면 CPU 컨볼루션이라 즉시성을 정면으로
  위협한다. 대신 같은 경로를 굵고 아주 옅게 한 겹 깔아 스민 느낌만 흉내낸다.

## 8. 아직 안 정한 것

- [ ] 프로젝트 이름 — 화면에는 "음정으로 그리는 그림"을 임시로 걸어두었다
- [x] ~~가이드 음 재생을 P0로 올릴 것인가~~ → **올린다**로 확정. 없으면 "심사위원이 직접 해본다"는
      시나리오 자체가 성립하지 않는다. 핵심 플레이 루프 스펙에서 해소되어 구현까지 끝났다.
