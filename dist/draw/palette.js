/**
 * 색 — 스테이지는 종이, 궤적은 잉크다.
 *
 * 어두운 판에 형광 선을 긋던 것을 뒤집었다. 이유는 미감이 아니라 셋이다.
 *   1. 판이 물체로 읽힌다. 뒤집기 전 스테이지와 페이지 배경의 대비는 1.03:1 이었다 —
 *      화면의 74%를 차지하는 판이 배경과 구별되지 않았다.
 *   2. 궤적이 계기판 색에서 떨어져 나온다. 전에는 궤적과 UI 액센트가 같은 값이라
 *      사용자가 그린 그림이 '지표'로 칠해져 있었다. 그래서 벗어남이 '서툼'이 아니라
 *      '오차'로 읽혔다 — 오차는 창피하고 서툼은 웃긴데, 우리가 원하는 건 후자다.
 *   3. 겹침이 공짜로 정보가 된다. 밝은 종이 위 반투명 잉크는 Normal 합성만으로도
 *      두 번 지난 자리가 진해진다. 어두운 배경에서 같은 걸 얻으려면 블렌드 모드가
 *      필요한데, 그건 이 앱에서 한 번도 실행해본 적 없는 API 다.
 *
 * ThorVG 가 CSS 배경 위에 투명 합성되는지는 확인된 적이 없다. 그래서 종이를
 * 렌더러가 도형으로 직접 깐다 — 합성 동작에 기대지 않는다.
 */
/**
 * `satisfies` 로 묶는다 — 색이 튜플 그대로 남아 렌더러가 `fill(...INK.pen)` 로 펼칠 수 있고,
 * 없는 이름을 적으면 그 자리에서 걸린다. `Record<string, Color>` 로 두면 둘 다 안 된다.
 */
export const INK = {
    /** 종이. CSS `#stage` 배경(`--paper`)과 같은 값이어야 로딩 중 색이 튀지 않는다. */
    paper: [236, 229, 216, 255],
    /**
     * 궤적 — 실제로 그려진 선. 알파를 255 로 두지 않는다.
     * 두 번 지난 자리가 진해져야 '잉크'로 보인다.
     */
    trail: [38, 30, 24, 224],
    /** 궤적 아래에 깔아 번짐을 흉내내는 겹. 굵고 아주 옅다. */
    trailBleed: [38, 30, 24, 40],
    /** 아직 지나가지 않은 목표선 — 연필로 그은 밑그림 */
    guidePathAhead: [150, 141, 126, 255],
    /** 이미 지나온 목표선 — 뒤로 물러난다 */
    guidePathPast: [193, 186, 172, 255],
    /** 결과 화면의 목표선. 비교 대상이므로 한 색으로, 또렷하게. */
    guidePathResult: [150, 141, 126, 255],
    /** 시작점 */
    start: [193, 65, 12, 235],
    /** 펜 */
    pen: [38, 30, 24, 255],
};
//# sourceMappingURL=palette.js.map